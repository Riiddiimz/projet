name: Col'inCall Next.js Migration QA

on:
  workflow_dispatch:
  push:
    branches:
      - migration-nextjs

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - name: Install dependencies
        run: npm install
      - name: TypeScript and Next build
        run: npm run build
